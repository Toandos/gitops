apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mail

resources:
  - namespace.yaml
  - stalwart-cluster.yaml

configMapGenerator:
  - name: stalwart-config
    files:
      - config.json

generatorOptions:
  disableNameSuffixHash: true