apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: identity
  namespace: flux-system
spec:
  interval: 10m
  path: ./platform/capabilities/identity
  prune: true
  wait: true
  dependsOn:
    - name: cloudnative-pg
    - name: external-secrets
    - name: kubernetes-replicator
  sourceRef:
    kind: GitRepository
    name: flux-system