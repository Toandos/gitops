apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: identity

resources:
  - helm-release.yaml
  - httproute.yaml

configMapGenerator:
  - name: kratos-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true