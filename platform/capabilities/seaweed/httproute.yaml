apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: seaweed-s3
spec:
  parentRefs:
    - name: public-gateway
      namespace: networking

  hostnames:
    - s3.toando.de

  rules:
      backendRefs:
        - name: seaweed-s3
          port: 8333