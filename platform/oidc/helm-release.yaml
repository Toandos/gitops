apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hydra
spec:
  interval: 30m
  chart:
    spec:
      chart: hydra
      version: "v0.63.0"
      sourceRef:
        kind: HelmRepository
        name: ory
        namespace: platform-sources

  valuesFrom:
    - kind: ConfigMap
      name: hydra-values
      valuesKey: values.yaml