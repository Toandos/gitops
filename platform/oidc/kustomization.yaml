apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oidc

resources:
  - namespace.yaml
  - helm-release.yaml
  - httproute.yaml

configMapGenerator:
  - name: hydra-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true