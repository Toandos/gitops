apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: permissions

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: keto-config
    files: ["config/keto.yaml"]
  - name: keto-namespaces
    files: ["namespaces/example.ts"]