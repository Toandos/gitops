apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ory-hydra
spec:
  parentRefs:
    - name: public-gateway
      namespace: networking

  hostnames:
    - oidc.toando.de

  rules:
    - backendRefs:
        - name: hydra-public
          port: 4444