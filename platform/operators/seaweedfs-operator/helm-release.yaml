apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: seaweedfs-operator
spec:
  interval: 30m
  chart:
    spec:
      chart: seaweedfs-operator
      version: 0.1.39
      sourceRef:
        kind: HelmRepository
        name: seaweedfs-operator

    install:
      crds: Create
    upgrade:
      crds: CreateReplace