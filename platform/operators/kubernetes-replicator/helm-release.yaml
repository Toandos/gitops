apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubernetes-replicator

spec:
  interval: 30m

  chart:
    spec:
      chart: kubernetes-replicator
      version: "2.12.4"
      sourceRef:
        kind: HelmRepository
        name: mittwald
  install:
    crds: Create

  upgrade:
    crds: CreateReplace