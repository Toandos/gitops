apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dashboard
spec:
  parentRefs:
    - name: public-gateway
      namespace: networking

  hostnames:
    - dash.toando.de

  rules:
    - backendRefs:
        - name: dashboard
          port: 3000