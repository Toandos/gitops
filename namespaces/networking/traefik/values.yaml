service:
    enabled: false

deployment:
    kind: DaemonSet

ports:
    web:
        port: 80
        hostPort: 80
        exposedPort: 80
    websecure:
        port: 443
        hostPort: 443
        exposedPort: 443
        
providers:
    kubernetesIngress:
        enabled: false
    kubernetesGateway:
        enabled: true