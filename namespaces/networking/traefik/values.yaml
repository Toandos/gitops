providers:
    kubernetesIngress:
        enabled: false
    kubernetesGateway:
        enabled: true
    
ports:
    web:
        port: 80
        expose:
            default: true

    websecure:
        port: 443
        expose:
            default: true