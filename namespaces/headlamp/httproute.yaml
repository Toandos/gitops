apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp
spec:
  parentRefs:
    - name: public-gateway
      namespace: networking

  hostnames:
    - headlamp.toando.de

  rules:
    - backendRefs:
        - name: headlamp
          port: 80